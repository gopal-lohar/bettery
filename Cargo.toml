[package]
name = "bettery"
version = "0.1.0"
edition = "2021"
description = "A minimal TUI battery manager for Linux"

[[bin]]
name = "bettery"
path = "src/main.rs"

[dependencies]
ratatui = "0.26"
crossterm = "0.27"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
anyhow = "1"
